export enum PAGES_URL {
  REGISTER = '/register',
  LOG_IN = '/log-in',

  PROFILES = '/profiles',
  USERS = '/users',
  DASHBOARD = '/dashboard',
  SETTINGS = '/settings',

  NOT_FROUND = '/not-found',
}
